# Create the real file OUTSIDE Git. Grant DNS edit + zone read only for your own zone.
apiVersion: v1
kind: Secret
metadata:
  name: am-cloudflare-dns
  namespace: cert-manager
type: Opaque
stringData:
  api-token: REPLACE_WITH_SCOPED_DNS_API_TOKEN
